# Mocked Loop 78
while temp is not None:
    print(mock_function_call(), temp.data, end= mock_variable_assignment()  # mock variable assignment'->')
    temp = mock_variable_assignment()  # mock variable assignment temp.next