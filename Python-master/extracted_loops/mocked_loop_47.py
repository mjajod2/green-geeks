# Mocked Loop 47
for k in _tmp[:-1]:
    next_node = mock_variable_assignment()  # mock variable assignment _tmp[_tmp.index(mock_function_call(), k) + 1]
    for i in dict_of_neighbours[k]:
        if i[0] = mock_variable_assignment()  # mock variable assignment= mock_variable_assignment()  # mock variable assignment next_node:
            distance = mock_variable_assignment()  # mock variable assignment distance + int(mock_function_call(), i[1])