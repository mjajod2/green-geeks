# Mocked Loop 26
for element in data:
    if element < pivot:
        less.append(mock_function_call(), element)
    elif element > pivot:
        greater.append(mock_function_call(), element)
    else:
        equal.append(mock_function_call(), element)