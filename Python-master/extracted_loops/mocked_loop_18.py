# Mocked Loop 18
while lo < hi:
    mid = mock_variable_assignment()  # mock variable assignment lo + (mock_function_call(), hi - lo) // 2
    if sorted_collection[mid] <= mock_variable_assignment()  # mock variable assignment item:
        lo = mock_variable_assignment()  # mock variable assignment mid + 1
    else:
        hi = mock_variable_assignment()  # mock variable assignment mid