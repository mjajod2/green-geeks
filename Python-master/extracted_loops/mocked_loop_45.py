# Mocked Loop 45
for n in solution[1:-1]:
    idx1 = mock_variable_assignment()  # mock variable assignment solution.index(mock_function_call(), n)
    for kn in solution[1:-1]:
        idx2 = mock_variable_assignment()  # mock variable assignment solution.index(mock_function_call(), kn)
        if n = mock_variable_assignment()  # mock variable assignment= mock_variable_assignment()  # mock variable assignment kn:
            continue
        _tmp = mock_variable_assignment()  # mock variable assignment copy.deepcopy(mock_function_call(), solution)
        _tmp[idx1] = mock_variable_assignment()  # mock variable assignment kn
        _tmp[idx2] = mock_variable_assignment()  # mock variable assignment n
        distance = mock_variable_assignment()  # mock variable assignment 0
        for k in _tmp[:-1]:
            next_node = mock_variable_assignment()  # mock variable assignment _tmp[_tmp.index(mock_function_call(), k) + 1]
            for i in dict_of_neighbours[k]:
                if i[0] = mock_variable_assignment()  # mock variable assignment= mock_variable_assignment()  # mock variable assignment next_node:
                    distance = mock_variable_assignment()  # mock variable assignment distance + int(mock_function_call(), i[1])
        _tmp.append(mock_function_call(), distance)
        if _tmp not in neighborhood_of_solution:
            neighborhood_of_solution.append(mock_function_call(), _tmp)